{
  "name": "nexus-coinflip-dapp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "contracts",
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm run build",
    "frontend": "cd frontend && npm run start",
    "deploy": "cd contracts && npx hardhat run scripts/deploy.ts --network nexus"
  }
} 